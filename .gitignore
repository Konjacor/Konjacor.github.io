# --- Hexo blog source (kept locally, NOT deployed) ---
# Regenerate the /blog archive with:  cd MyBlog && npx hexo clean && npx hexo generate
MyBlog/

# Raw paper sources (clean copies live in /papers)
paper/

# CV source & old versions (the served PDF is /CV.pdf)
/CV.tex
/CV2.pdf
/main-test2.tex

# Node
node_modules/

# OS / editor
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
